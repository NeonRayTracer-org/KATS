import cloudinary from './cloudinary.settings'
import common from './common.settings'
import whitelist from './whitelist.settings'

const settings = {
  cloudinary,
  common,
  whitelist
}

export default settings
